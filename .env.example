# Backend Configuration
PORT=8000
DATABASE_URL=mongodb://localhost:27017/talkative
FRONTEND_SERVER_URL=http://localhost:3000

# Ollama Configuration
# When running locally (outside Docker), use localhost
# When running in Docker, this is overridden in docker-compose.yml to use host.docker.internal
OLLAMA_BASE_URL=http://localhost:11434

# Optional: JWT Secrets etc if needed (based on package.json dependencies like jsonwebtoken)
JWT_SECRET=your_jwt_secret_here
